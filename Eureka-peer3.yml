# ==============================================================
# = Eureka Peer 3 Parameters
# ==============================================================


server:
  port: 8767
  tomcat:
    threads:
      max: 5

eureka:
  instance:
    hostname: peer3

  client:
    serviceUrl:
      defaultZone: http://peer2:8766/eureka/, http://peer1:8765/eureka/