# ==============================================================
# = Eureka Peer 1 Parameters
# ==============================================================


server:
  port: 8765
  tomcat:
    threads:
      max: 5

eureka:
  instance:
    hostname: peer1

  client:
    serviceUrl:
      defaultZone: http://peer2:8766/eureka/, http://peer3:8767/eureka/