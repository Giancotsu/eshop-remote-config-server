# ==============================================================
# = Eureka Peer 2 Parameters
# ==============================================================


server:
  port: 8766
  tomcat:
    threads:
      max: 5

eureka:
  instance:
    hostname: peer2

  client:
    serviceUrl:
      defaultZone: http://peer1:8765/eureka/, http://peer3:8767/eureka/